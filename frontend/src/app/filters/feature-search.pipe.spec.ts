import { FeatureSearch } from './feature-search.pipe';

describe('FeatureSearch.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new FeatureSearch();
    expect(pipe).toBeTruthy();
  });
});
